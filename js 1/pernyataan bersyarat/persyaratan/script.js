const angka = 30;
		if(angka > 15 && angka < 50){
			document.writeln("Angka lebih dari 2 digit");
		}

		const angko = 50;
		if (angko < 40 || angko > 30) {
			document.writeln("Angka lebih dari 2 digit");
		}

		// latihan
		const age = 24;